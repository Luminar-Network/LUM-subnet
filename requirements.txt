# Minimal working requirements for Python 3.13
# Core Bittensor and Database
bittensor>=7.0.0
asyncpg>=0.28.0
psycopg2-binary>=2.9.0
sqlalchemy>=2.0.0

# Essential Python packages  
pydantic>=2
rich>=13
pytest>=8
numpy>=1
setuptools~=70.0.0

# Data processing (working versions)
pandas>=2.0.0
python-dateutil>=2.8.0
geopy>=2.3.0

# Web and API
requests>=2.30.0
aiohttp>=3.8.0
fastapi>=0.100.0
starlette>=0.30.0

# Security and utilities
cryptography>=40.0.0
python-dotenv>=1.0.0
tenacity>=8.2.0
asyncio-throttle>=1.0.0

# Optional: Add these later when Python 3.13 support improves
torch>=2.0.0              # Large package, install separately if needed
# transformers>=4.20.0      # Has numpy version conflicts
# scikit-learn>=1.3.0       # Build issues with Python 3.13
# sentence-transformers>=2.2.0  # Depends on problematic packages
# torchvision>=0.15.0       # Depends on torch
# opencv-python>=4.8.0      # May have build issues
# clip-by-openai>=1.0       # Depends on torch
openai>=1.0.0
# pillow>=9.0.0             # Can install separately

pillow>=11.0.0